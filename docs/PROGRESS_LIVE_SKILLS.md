# Progress note: PRD_LIVE_SKILLS implementation

Work in progress on `docs/PRD_LIVE_SKILLS.md` (live Pi and Claude skills). Committed mid-slice so it can be resumed.

## Done

- **Read-only discovery** (2026-09-20): personal `~/.pi/agent/skills`; packages `~/.pi/agent/npm/node_modules/{pi-subagents,pi-mcp-adapter}/skills`; `~/.claude/skills` (synced collection nested inside); synced plugins `~/.claude/plugins/synced/<id>/{cowork-plugin-management,design}/skills`; 20 marketplace plugin `skills` dirs under `~/.claude/plugins/marketplaces/claude-plugins-official/{plugins,external_plugins}/*/skills`; project `~/dev/agent-workflow/.pi/skills`. No symlinked candidates. Live config not yet written (`~/.config/md-manager` does not exist).
- **Slice 1 (config registry) — green.** `server/config.ts` + `server/config.test.ts` (9 tests). Red evidence: first run `ERR_MODULE_NOT_FOUND`, then 9 behavioural failures against a skeleton, then 9 passed. Contract: `MD_MANAGER_CONFIG` (JSON, version 1, absolute paths, ids `^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$`, categories personal/package/plugin/project, duplicate ids and overlapping/aliased roots rejected via realpath), default `$HOME/.config/md-manager/sources.json`, `MD_MANAGER_FIXTURE_ROOT` → ids `pi-fixtures`/`claude-fixtures`, both set = error, missing config = startup error (no fixture fallback). `loadConfig(env)` takes an explicit env object.

## In progress: Slice 2 (multi-root backend) — RED, server not yet changed

- `server/app.test.ts`, `file.test.ts`, `write.test.ts`, `mutate.test.ts` migrated to the location contract: `createApp(fixtureLocations(root))`, requests/responses carry `locationId` (`destinationLocationId` for copy), `GET /api/file?source&locationId&path`, listing returns `{ locations: [{id, source, label, category, status, error}], entries: [{source, locationId, path, kind}] }`, per-location `unavailable` status instead of a whole-listing 500, `.git` excluded at any depth, other dot dirs included. `mutate.test.ts` imports `LocationRegistry` from `./registry.ts` (not yet written).
- Still to add: `server/locations.test.ts` (multi-location: duplicate relative paths, unknown/mismatched location → 400 `INVALID_LOCATION`, unavailable location → 404 `LOCATION_UNAVAILABLE`, `.git` denied for mutations, cross-location move refused, copy needs `destinationLocationId` under the other source, root replaced after open never redirects an in-flight read, handles closed on close/failed init, unreadable/symlinked root recovery on refresh).
- Then implement: `server/registry.ts` (per-location handle, lazy reopen/revalidate via fstat vs lstat dev/ino inside the mutation queue, capability probe before opening roots), refactor `server/files.ts` (`withParentDirectory(location, components, op)`, no `pi/`/`claude/` mapping, reject `.git` components), `server/mutations.ts`, `server/app.ts` (`createApp(locations, options)`), `server/index.ts` (`loadConfig(process.env)`, exit 1 with diagnostic on `ConfigError`, log per-location status). Update `package.json` `test:unit` to include `server/config.test.ts` and `server/locations.test.ts`.

## Remaining slices (plan)

- **Slice 3 (UI):** node ids `Pi`, `Pi/<locId>`, `Pi/<locId>/<path>`; refs gain `locationId`; URLs `/browse/<source>[/<locId>[/segments]]` and `/file/<source>/<locId>/segments`; legacy `/<Source>/...` and `/file/<Source>/<x.md>` show an explanation with per-location links (never auto-resolved); location nodes in graph/outline with unavailable state; breadcrumbs Home/Source/Label/…; creation at a source node requires choosing a location; move stays in one location; copy chooses destination location + folder; package/plugin notice (`data-testid="installed-notice"`); Refresh keeps drafts.
- **Browser harness:** `playwright.config.ts` creates a temp root (copies `fixtures/pi` → `pi-personal`, `fixtures/claude` → `claude-personal`, adds `pi-package` and `claude-plugin` each with `review/SKILL.md`, plus a missing `pi-missing` project location), writes `sources.json`, sets `MD_MANAGER_CONFIG`, removes it in `globalTeardown`. All existing specs must migrate ids/URLs; scratch files go under the temp roots, never `fixtures/`. Test ports: `MD_MANAGER_WEB_PORT=5184 MD_MANAGER_API_PORT=3014` (preview 4184); dev servers on 5173/3001 stay running.
- **Slice 4:** lint/build/preview gates, `config/sources.example.json`, live `~/.config/md-manager/sources.json` from the discovery above (individual package/plugin skills dirs, stable ids), README (setup, restart, SSH tunnel `ssh -L 5173:127.0.0.1:5173 -L 3001:127.0.0.1:3001 …`, loopback only), `docs/HANDOFF_LIVE_SKILLS.md` with red/green evidence, read-only smoke check against the live config.
