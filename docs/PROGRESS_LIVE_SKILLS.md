# Progress note: PRD_LIVE_SKILLS implementation

Work in progress on `docs/PRD_LIVE_SKILLS.md` (live Pi and Claude skills). Committed mid-slice so it can be resumed.

## Done

- **Read-only discovery** (2026-09-20): personal `~/.pi/agent/skills`; packages `~/.pi/agent/npm/node_modules/{pi-subagents,pi-mcp-adapter}/skills`; `~/.claude/skills` (synced collection nested inside); synced plugins `~/.claude/plugins/synced/<id>/{cowork-plugin-management,design}/skills`; 20 marketplace plugin `skills` dirs under `~/.claude/plugins/marketplaces/claude-plugins-official/{plugins,external_plugins}/*/skills`; project `~/dev/agent-workflow/.pi/skills`. No symlinked candidates. Live config not yet written (`~/.config/md-manager` does not exist).
- **Slice 1 (config registry) — green.** `server/config.ts` + `server/config.test.ts` (9 tests). Red evidence: first run `ERR_MODULE_NOT_FOUND`, then 9 behavioural failures against a skeleton, then 9 passed. Contract: `MD_MANAGER_CONFIG` (JSON, version 1, absolute paths, ids `^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$`, categories personal/package/plugin/project, duplicate ids and overlapping/aliased roots rejected via realpath), default `$HOME/.config/md-manager/sources.json`, `MD_MANAGER_FIXTURE_ROOT` → ids `pi-fixtures`/`claude-fixtures`, both set = error, missing config = startup error (no fixture fallback). `loadConfig(env)` takes an explicit env object.

- **Slice 2 (multi-root backend) — green** (2026-09-20). Red: 55 failing / 11 passing with the migrated suites against the old server (`scratchpad/slice2-red-2.txt`: `createApp(fixtureLocations(...))`, `locationId` in every request/response, per-location listing; `mutate.test.ts` and the new `server/locations.test.ts` could not load `server/registry.ts`). Green: 102/102 server tests, then `npm run test:unit` 126/126, `tsc -p tsconfig.node.json`, `eslint server`, `npm run build` all clean. Implementation: `server/registry.ts` (`LocationRegistry`: capability probe on `open()`, lexical `resolve(source, locationId)` → 400 `INVALID_LOCATION`, `withRoot()` opens the trusted configured path `O_NOFOLLOW|O_DIRECTORY` for exactly one operation → 404 `LOCATION_UNAVAILABLE` for ENOENT/ENOTDIR/ELOOP/EACCES with a path-free reason (symlink vs not-a-folder told apart by lstat), `list()` walks each root through no-follow descriptors, skips `.git` and symlinks, reports `{locations:[{id,source,label,category,status,error}], entries}`), `server/files.ts` (no `pi/`/`claude/` mapping; `.git` component → 400 `INVALID_PATH` on every route; `withParentDirectory(root, components, op)`), `server/mutations.ts` (`locationId`/`destinationLocationId`; rename/move refuse a different `destinationLocationId`; copy resolves the destination location and opens its root only after the source file was read), `server/app.ts` (`createApp(locations, options)`), `server/index.ts` (`loadConfig(process.env)`, exit 1 with the ConfigError text, per-location startup log). Decision recorded: a symlinked/missing/unreadable **root** is `LOCATION_UNAVAILABLE` (404) on every route, not `NOT_FOUND`; two migrated assertions in `write.test.ts`/`mutate.test.ts` were updated accordingly. Design change from the earlier plan: no retained per-location handle or fstat/lstat revalidation; opening the root per operation gives the same pinning guarantee with nothing to leak or reopen. `package.json` `test:unit` now includes `server/config.test.ts` and `server/locations.test.ts`. Side effect: the `npm run dev` API watcher on 3001 now exits at startup until a live config exists (Slice 4); Vite on 5173 keeps running.

- **Slice 3 (UI) — green** (2026-09-20). Model red: `tests/unit/model.test.ts` rewritten for `(source, locationId, path)` identities, `/browse` and `/file/<source>/<loc>` URLs, legacy links, location breadcrumbs and counts; 11/12 failed behaviourally against a skeleton export, 12/12 after the new `src/graph/model.ts`. UI: location nodes (rounded rectangles, dashed + "unavailable" tag) in graph and outline, `Breadcrumbs`/`OperationDialog`/`DocumentView`/`App` location-aware, explicit location choice for creation at a source node and for copy destinations, installed-file notice, legacy/unconfigured-location explanations with per-location links. Harness: `playwright.config.ts` builds one temp root per run (`pi-personal`, `pi-package`, missing `pi-missing`, `claude-personal`, `claude-plugin`), writes `sources.json`, passes `MD_MANAGER_CONFIG` to the API, `tests/global-teardown.ts` removes it; `tests/helpers.ts` exports ids/labels/roots/URL builders and `expandLocation`/`fit`; every spec migrated; new `tests/locations.spec.ts`. Browser red 86/105 (`e2e-1.txt`), then 102/105, then **105/105 (dev, 5184/3014)**. Product fixes found by the browser reds: labels moved out of the focusable node body (multi-line labels put the pointer target in the gap under the shape), breadcrumb visible limit 4 → 5, pending focus retried after the layout places new nodes (direct-link Back to folder never focused the file node before). Test-authoring slips fixed separately: `.nth(1)` on an already-expanded folder, a substring assertion matching "skills" inside a location label, a forbidden-but-harmless 400 request on a legacy link, a trailing newline in a seeded draft file.
- **Slice 4 (rollout) — done.** Preview e2e 105/105 (4184/3014). `config/sources.example.json`; live `~/.config/md-manager/sources.json` generated from the read-only inventory (25 locations: 4 Pi, 21 Claude, stable ids `pi-personal`, `pi-pkg-*`, `pi-project-agent-workflow`, `claude-personal`, `claude-synced-*`, `claude-marketplace-*`); read-only smoke check on port 3015: 25 available, 307 entries, 162 files, representative reads 200 across all categories, no absolute paths in responses. README rewritten. `docs/HANDOFF_LIVE_SKILLS.md` holds the evidence. Gates: `npm run test:unit` 130/130, `tsc -b`, `eslint .`, `npm run build`, `git diff --check`, `fixtures/` clean, dev e2e 105/105. Commits left to the user.

## Remaining

- Nothing planned. Restart `npm run dev` so the API picks up the live config (the watcher exited at startup while no config existed).
