# Adapter worker: not selected for this follow-up

This follow-up run launches only the `ui` lane (`--workers ui`). The contract, server and controller work landed by run
`parallel-reviewers-001` is the fixed base of this run. This file exists because the feature manifest declares both lanes.
